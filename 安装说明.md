# 《生存日志》食材来源扩展 Mod（CookingSourceExpand）

兼容 Steam 版《Survival Log》，内置 BepInEx 6（IL2CPP 版，be.785），已含编译好的 `CookingSourceExpand.dll`，解压即用，无需安装任何环境。

当前版本：**v1.3.2**

### 更新日志（v1.3.2）

- **按「当前角色家」枚举**：烹饪/手工/工作台/交易四个界面统一改为只用 `GetFurnituresWithBag(当前家)` 枚举，杜绝多角色存档里其它角色家（打工人/女大学生互串）的柜子混入面板。
- **名字级排除（跨角色生效）**：门窗、床（以“床”结尾，不误伤床头柜）、围栏、栅栏、燃气灶、微波炉、酿酒桶、榨汁机、吊篮等一律不进入来源，不再依赖固定 configId，各角色统一生效。该角色家里不同 configId 的同类家具也会被按名字自动拦下。
- **白名单扩充**：并入实测的储物柜/架/箱/冰箱/书架/橱柜/吊篮/酿酒桶/置物架 id，保证女大学生等角色的工作台跨面板取料可用。

## 功能

该 Mod 同时扩展 **四个界面的食材/材料来源**：

1. **烹饪面板（灶台 / 火炉）**：可调用的食材来源，从"仅有冰箱、冷柜"扩展为"基地内所有带储物功能的家具"（箱子、柜子、储物架、冰箱、冷柜等）。
2. **手工制作界面**：可调用的材料来源，从"仅有工作台抽屉/工具柜"扩展为"所有带储物功能的家具"，手工做物品时可直接从各处储物容器取料。
3. **工作台（ToolTable）**：工作台自动填料可跨面板从各处储物家具取料，不必切到对应容器。
4. **无人机交易界面（交易/捐赠/供给）**：可调用来源从"仅有背包/无人机本体"扩展为"所有带储物功能的家具"，多个储物柜之间可直接互通取料。

做菜/手工/工作台/交易前无需把食材先搬到特定容器，直接从各处储物柜取用即可。

### 归属过滤（v1.3.0 新增）

仅将**当前角色家**（含二楼、地下室）的储物家具作为来源，自动排除其它角色（如女大学生）家里的柜子，避免串料。

### 来源过滤配置（v1.3.1 新增）

箱子太多、在各界面点击找箱子太慢时，可编辑 `BepInEx\config\CookingSourceExpand.cfg`（首次运行自动生成，或手动新建）：

- `Sources > IncludeBoxConfigIds`：**白名单**。填入箱子类型 id（英文逗号分隔，如 `3001,80156`）后，只有这些类型的箱子会作为来源；留空=使用全部默认储物箱子。
- `Sources > ExtraExcludedBoxConfigIds`：**额外排除**。填入的箱子类型 id 总是被排除（即使命中默认白名单）。

示例：

```ini
[Sources]
# 只保留 箱子3001 和 储物柜
IncludeBoxConfigIds = 3001,80156
# 同时把 矮柜 排掉
ExtraExcludedBoxConfigIds = 1006
```

这是一个纯后端的数据过滤，不触碰游戏前端界面，稳定可靠，游戏更新后也不会失效。

### 静默无窗（v1.3.1）

发布包已预置 `BepInEx\config\BepInEx.cfg`，默认关闭启动控制台窗口，游戏启动不再弹出黑色后台窗口，日志仍写入 `BepInEx\LogOutput.log` 供排查。

### 排除名单

已自动排除非储物容器：房门、木门、铁门、大门、窗户、木窗、铁窗、玻璃窗、铝合金窗、防盗门、防盗窗、小汽车、无人机、行李箱、植物灯培育箱。

## 安装步骤

1. Steam 中右键《Survival Log》→ 管理 → 浏览本地文件（找到 `SurvivalLog.exe`）。
2. 解压本压缩包 → 把里面**所有内容**（`winhttp.dll`、`doorstop_config.ini`、`BepInEx`、`dotnet`）复制到游戏根目录，同名选"覆盖/合并"。
3. 从 Steam 启动游戏，读完档后打开灶台/火炉烹饪面板、手工制作界面、工作台或无人机交易界面，即可看到多个储物来源可选择。

## 卸载

删除 `游戏根目录\BepInEx\plugins\CookingSourceExpand.dll` 即可。若要撤销全部：删除 `winhttp.dll`、`doorstop_config.ini`、`dotnet`、`BepInEx` 这几个。

## 交互兼容

- 与储物扩容 Mod（BagSizeExpand）可同时使用，互不干扰。
- 若想增删排除项，需修改源码 `CookingBagPatch.cs` 中 `ExcludedSourceNames` 后重新编译。

## 注意事项

- 本包对应游戏版本 **v1.0.14911（IL2CPP 元数据 v31）**。
- 首次启动会较慢（BepInEx 生成兼容层，约 1~3 分钟），属正常现象。
- 使用前建议备份存档。

## 关于游戏更新

本 Mod 已做**抗更新加固**：补丁改为运行时显式查找目标方法，目标方法因游戏版本更新而消失或改变时，对应功能会自动静默停用并写入日志，**不会导致游戏启动失败或闪退**。若某个界面在更新后不再出现多个储物来源，查看 `BepInEx\LogOutput.log` 中 `[CookingSourceExpand]` 相关日志即可确认是否因更新而停用。