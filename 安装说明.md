# 《生存日志》BaseButler 安装说明

当前版本：**v2.0.0** · GUID：`com.basebutler.mod`

BaseButler 是整合插件：**食材/杂物来源扩展（含一键烹饪）** + **仓储管家** + **堆叠优化**，三模块共用一个 DLL。兼容 Steam 版《Survival Log》的 BepInEx 6（IL2CPP）。

> 本仓库为**源码库**，不含预先打包好的 BepInEx 框架。需要即用发行包（含完整 BepInEx）的请看 N 网对应发布，或按下面流程自行编译部署。

## 安装

1. 备份并删除 `BepInEx\plugins\` 下旧版 `CookingSourceExpand.dll`（避免与整合版冲突）。
2. 将 `BaseButler.dll` 放入 `BepInEx\plugins\`。
3. 从 Steam 启动游戏。首次启动会多花 1~3 分钟（BepInEx 兼容层生成），属正常。
4. 首次运行后生成 `BepInEx\config\BaseButler.cfg`，可按需调整参数。

## 关键配置

编辑 `BepInEx\config\BaseButler.cfg`：

- `Sources > IncludeBoxConfigIds`：**来源白名单**。填入箱子类型 id 后，只有这些类型作为来源；留空=全部默认储物箱子。
- `Sources > ExtraExcludedBoxConfigIds`：**额外排除**。这些箱子类型 id 总是被排除。
- `Sources > TreatAsCookingBag`：是否把联动储物柜放行为"烹饪可直取袋"（默认开）。
- `Sources > SelectRecipeDiagnostics`：点配方取料诊断日志（默认关，排查取料问题时开启）。

## 怎么确认装好了

- 游戏根目录生成了 `BepInEx\LogOutput.log`；
- 烹饪/手工/工作台/无人机交易面板能看到多个可选的储物来源。

## 卸载

删除 `游戏根目录\BepInEx\plugins\BaseButler.dll` 即可；若为旧 CookingSourceExpand 升级，同时删除 `CookingSourceExpand.dll`。

## 关于游戏更新

补丁采用运行时显式查找目标方法（SAFE 加载）：目标方法因更新消失或改变时，对应功能自动静默停用并写日志，**不会导致启动失败或闪退**。若某界面更新后不再多来源，查 `BepInEx\LogOutput.log` 中 `[CookingSourceExpand]` 相关日志即可。

详细功能见 [README.md](README.md)。